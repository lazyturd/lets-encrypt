apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-staging
spec:
  acme:
    email: user@example.com
    server: https://acme-staging-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-staging
    solvers:
    - dns01:
        route53:
          hostedZoneID: Z0600261OY5QAKYH2M34
          region: us-east-1
      selector:
        dnsZones:
        - "ayomide.shop"
    - http01:
        ingress:
          class: nginx